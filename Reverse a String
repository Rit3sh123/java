class ReverseAString {
    public static void main(String[] args) {
    
    String s="selenium";
    int len=s.length()-1;
    
    String rev="";
    for(int i=len;i>=0;i--)
    {
        rev=rev+s.charAt(i);
    }
    System.out.println("reverse string is:"+rev);
    
    }
}
